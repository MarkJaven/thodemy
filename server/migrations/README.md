# Migrations

Use this directory to store database migration scripts.

Suggested naming pattern:
- `YYYYMMDDHHMM_description.sql`

Keep each migration idempotent where possible and document any manual steps in the
migration file header.
